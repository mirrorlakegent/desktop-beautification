# 2026-08-10 工作日志

## 完成：Round 4 真机验收（挂载父窗口修复生效）
- 用户真机跑 `58d99d2` 构建（D:\WorkBuddy\ds\DesktopSuite.dll 00:09 / 160256 字节）。日志铁证：`FenceLayer.CreateDesktopWindow：ResolveDesktopHost => 0x2049A` 且 `ok host=0x2049A hwnd=0xF07B8 size=1920x1080`（第二次启用同样 0x2049A）。
- `0x2049A` = 视频壁纸渲染所用的 **WorkerW**（被 DWM 合成的表面）；此前四轮失败时为 `0x101CA`（Progman 直接子窗口，DWM 不合成）。**证明 Round 4 根因（挂载点选错）已修复**。
- 日志全程无报错；`EnableFences：完成` 两次；z-order 修复（围栏 HWND_TOP）保证叠在壁纸之上。
- 待用户最终视觉确认：5 个深色分类盒子（未分类/工作/娱乐/工具/临时）是否出现。
  - 若可见 → 横跨 4 轮的「空白桌面」bug 收官；更新 08-09 记忆标记 VERIFIED，收尾团队 `gstack-fence-callpath`。
  - 若仍空白（host 已正确为 WorkerW）→ 不再是挂载/合成问题，转区域/裁剪/DPI 问题（ApplyRegion）；下一步重开 `_diagFullWindow=true` 全屏诊断覆盖层：能显示则命中区逻辑错，不能显示则更深。

## 进行中：Round 4-bis 诊断（用户确认 B）
- 用户回 **B**：host=0x2049A（WorkerW，Round 4 挂载修复生效）但屏幕**仍空白** → 问题已排除挂载/合成，转向 `ApplyRegion` 的命中区/裁剪/DPI（窗口建对地方，但可见区域算错/被裁掉，或盒子矩形坐标错位）。
- 已建团队 `gstack-fence-diag`，派 gstack-investigator：① `_diagFullWindow` 改 `true` 重开全屏诊断覆盖层；② ApplyRegion 加命中区日志（盒子矩形数 N、各 rect bounds、_winW/_winH、是否命中"无命中区回退全屏"安全网）；③ 分析坐标体系（FIX-2 DPI 缩放 / 客户端坐标是否未转屏幕坐标 / union 空）。
- 待 investigator 回传 diff+分析 → 主理人构建自包含包部署 D:\WorkBuddy\ds → 用户用 go.bat 验收：
  - 全屏深色覆盖层**出现** → 合成/可见正常，锁定命中区逻辑 bug（坐标未转/DPI 因子错/union 空）；
  - 全屏覆盖层**仍不出现** → 更深：TryFindWallpaperSurface 返回的 WorkerW 非真正被 DWM 合成者，或壁纸停止后 WorkerW 被销毁/重建。

## 完成：Round 4-bis 诊断构建部署（待用户验收）
- 排障手改动：`FenceLayer._diagFullWindow` 改 `true`（重开全屏诊断覆盖层）+ `ApplyRegion` 前插入命中区诊断日志块（winW/winH/dpiX/dpiY/virtualLeft/Top/boxes 数；每个盒子 rect 物理像素 + emptyOrNeg；add-tile rect；union bounds/area/insideWindow；无命中区回退日志补 winW/winH）。Debug 编译 0 错误 0 警告（CS0136 局部变量重名已修，诊断块内 tlx/tly 改名 dtlx/dtly）。
- 编译曾遇 CS0136（诊断块 `tlx/tly` 与下方真实循环同名局部冲突）→ 改名解决，唯一编译问题。
- 主理人发布自包含包部署 `D:\WorkBuddy\ds\`：`DesktopSuite.dll` 时间戳 `22:13`、大小 `163328` 字节（含诊断日志，较 160256 增大）。`FenceLayer.cs` 为临时诊断改动，**未提交**（待二分结论后再决定提交真实修复或回退）。
- 排障手关键分析：**本机 96 DPI（100% 缩放）**，host=0x2049A 正确、无报错前提下，区域数学（cat.X/Y 为 virtual-screen 物理像素，区域 left=cat.X-virtualLeft，与 WPF 渲染位置对齐）内部自洽 → 反而**更指向"窗口未被 DWM 合成"而非命中区裁掉**。覆盖层测试即为此二分。
- 用户验收步骤：双击 `go.bat` → 点「启用桌面整理」→ 看**全屏深色覆盖层**是否出现，并 grep `wallpaper.log`：
  - 出现 → 锁定命中区逻辑：查 `HIT[0..4]` 的 `emptyOrNeg`（盒子原始 Width/Height 可能为 0/负，被 CreateRectRgn 排除）、`HIT union insideWindow`（矩形是否落窗外）、`combined==null`（_boxes 为空）。
  - 不出现 → 更深：WorkerW 非真正合成表面 / 壁纸停后 WorkerW 重建 / Z-order；下一轮需在 Show 后补 `WorkerWHost.Strategy` + handle/rect/IsWindow 探针，或对照实验改回挂 Progman 本体隔离"哪个父窗口被 DWM 合成"。

## 关键转折：用户确认 B（全屏覆盖层仍不出现）→ 根因转向「围栏窗口自身样式」
- 决定性事实：**视频壁纸的子窗口（0x1507DC，mpv）挂在 0x2049A 上能正常显示**，而围栏（同样挂在 0x2049A 的子窗口）即便设了整窗全屏可见区域也不显示。
- ⇒ 父窗口 0x2049A **是被 DWM 合成的**（壁纸能证明）；围栏不显示**不是挂载点问题**，而是**围栏窗口自身的 Win32 属性**阻止了 DWM 合成它（如 WS_EX_LAYERED 子窗口 DWM 跳过合成 / 缺 WS_VISIBLE / 被同级兄弟窗口 Z-order 压住 / WPF _root 实际尺寸为 0 未绘制）。
- 排障手前一轮"情形 B = WorkerW 非合成表面"的假设**被壁纸反证削弱**；新方向是围栏窗口自身样式而非父窗口。
- 下一轮探针（派 gstack-investigator 继续）：在 `FenceLayer.CreateDesktopWindow` 取得 `_hwnd` 后，记录并解码：
  - `GetParent(_hwnd)`（确认确为 0x2049A，reparent 未失败）；
  - `GetWindowLongPtr(_hwnd, GWL_STYLE)` / `GWL_EXSTYLE`（解码 WS_VISIBLE/WS_CHILD/WS_EX_LAYERED/WS_EX_TRANSPARENT/WS_EX_TOOLWINDOW/WS_EX_NOACTIVATE）；
  - `IsWindowVisible`、`GetWindowRect` + 客户区；
  - 枚举 0x2049A 的**所有子窗口**，对每个记录 hwnd + `GetClassName` + GWL_STYLE/EXSTYLE（直接对比壁纸 mpv 子窗口与围栏子窗口的样式差）；
  - `DwmIsCompositionEnabled`；围栏 `_root.ActualWidth/ActualHeight`（确认 WPF 视觉是否真有尺寸在绘制）。
  - 若方便，可加对照实验：同一构建内额外把围栏也试挂 Progman 本体(0x101CA)，日志记哪种父窗口下可见，隔离变量。

## 完成：样式探针构建部署（待用户跑 + 读日志）
- 排障手在 `FenceLayer.CreateDesktopWindow` 后加 Probe A（raw pre-fixup：GetParent + GWL_STYLE/EXSTYLE，查 HwndSource 初始是否带 WS_EX_LAYERED）+ Probe B（final：style/ex/visible/GetWindowRect/客户区/DwmIsCompositionEnabled/强制 Measure-Arrange 后 _root.ActualWidth/Height + **递归枚举 0x2049A 全部子孙窗口**类名+样式+可见性，z-order 自顶向下）；`FenceNative.cs` 新增 GetParent/GetWindow/GetClassName/IsWindowVisible/DwmIsCompositionEnabled + DecodeStyle/DecodeExStyle。Debug 编译 0 错误。
- 主理人发布部署 `D:\WorkBuddy\ds\`：`DesktopSuite.dll` 22:41 / 166912 字节。FenceLayer.cs + FenceNative.cs 为未提交临时诊断改动（连同上一轮 _diagFullWindow=true + HIT 日志）。
- 排障手排序假设：**H1（最可疑）= z-order，mpv 壁纸作为 0x2049A 最顶子窗口把围栏整窗盖住**（完美解释 B：连全屏深色也被遮死）；H2=WS_EX_LAYERED 残留（改 EXSTYLE 后缺 SWP_FRAMECHANGED 让 DWM 重评估）；H4=缺 WS_VISIBLE；H5=_root 尺寸 0 未绘制；H3(WS_EX_TRANSPARENT) 基本排除（它反而强制置顶，不会让不透明窗口消失）。
- 用户读日志（wallpaper.log）一锤定音：
  - `PROBE B hostChildren` 列表——若 `cls=mpv/SDL/壁纸类` 出现在围栏 `cls=HwndWrapper...` **之前**（更靠上）→ H1 实锤 → 修"置顶时机/壁纸就绪后重顶"。
  - `PROBE B (final) ex=` 仍含 `WS_EX_LAYERED` → H2 → 补 `SetWindowPos(..., SWP_FRAMECHANGED)`。
  - `visible=False` / style 缺 `WS_VISIBLE` → H4。
  - `rootActual=0x0` → H5（查 HwndSource 尺寸/布局）。
  - 以上都正常仍不显示 → DWM 对 WPF HwndSource 子窗口的特例，需另隔离。
