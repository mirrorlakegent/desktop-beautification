# 2026-09-01 工作日志

## Route B（M4-B Phase 2）落地 — v26 (c563de0)
按路线图推荐序列，Route A(v25h) 之后开干 Route B 外观深化 + 外观预设：
- FenceAppearance 增 8 属性：BoxShadowEnabled/ShadowOffset/ShadowBlur/ShadowOpacity、BorderColorR/G/B、BorderOpacity、TitleFontFamily。
- AppSettings 同步 7 属性 + clamp；TitleFontFamily 经白名单校验（防 `new Font` 抛异常）。
- FenceLayer.DrawBoxShadow：白遮罩→BoxBlur→ColorMatrix 按模糊强度着色（阴影色复用边框色）。
  完全隔离于主体 alpha 管线，不触碰 v17 根治的 CreateDIBSection 路径（规避白 alpha 回归）。
- 边框：BorderOpacity>=16 时用自定义色绘制，否则沿用随主体透明的默认灰边。
- 字体：标题改读 TitleFontFamily（默认 Segoe UI，外观不变）；白名单=Segoe UI/Microsoft YaHei UI/微软雅黑/SimSun/Consolas 等。
- FencePresetStore（具名预设 JSON，%LocalAppData%/DesktopSuite/appearance-presets）+ InputBox。
- 托盘「🎨 外观预设」子菜单（打开时重建）+ 弹窗下拉可存可取；内置 默认/玻璃拟态/极简线框 三预设。
- Phase 0 发布安全网脚本 publish-ds2.ps1（解锁DLL→归档→单文件发布→双远程 ls-remote 校验）。
- 提交 c563de0，已推送 gitee+github 双远程（ls-remote 双方一致）。

## 待办 / 风险
- **Route B 待用户真机复验**（verify-first）：阴影/边框/字体/预设四项 + Fences 透明度回归矩阵
  （BodyOpacity=0/15/255、HeaderOpacity=0/10/30、Frosted 开）。
- 内置预设「极简线框」用 BodyOpacity=0 + BorderOpacity=160，需确认白色 alpha 管线不被触碰（理论上安全）。
- 下一步推荐序列：Phase 3 主题引擎贯通（脚手架已存在，映射 ResolvedTheme→FenceAppearance）。

## 环境注
- 沙箱系统时钟与本机不一致（提交/构建戳 08-31，会话 current_time 显示 09-01）；记忆文件按会话 current_time 命名。
