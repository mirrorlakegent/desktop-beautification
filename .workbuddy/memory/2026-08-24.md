# 2026-08-24 工作日志

## M4-B 透明度修复 v15
- **v14 失败根因**：`ClearBodyPixels()` 在 `FillBodyPixels` 正确写入主体像素（alpha=BodyOpacity）
  之后，无条件地将所有主体区域覆盖为 (0,0,0,1)，完全抹掉正确 alpha → 无论滑块多少都全白。
- **v15 修复**：删除 `ClearBodyPixels` 调用和方法体。恢复 v13 的正确管线：
  `FillBodyPixels`(LockBits 预填充) → GDI+ 绘制内容 → `PremultiplyAlpha` → `GetHbitmap` → DWM。
- 编译 0 错误，发布 ds2，双远程推送（66d9daf..dbcb8e5）
- **待用户重启复验**：透明度滑块应恢复正常响应（v13 水平 + alpha=0→1 GetHbitmap 绕过）
