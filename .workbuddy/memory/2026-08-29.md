# 2026-08-29 工作日志

## v25 毛玻璃修复（提交 dee9b97，用户复验：不再全白，但有残留图像）
- **根因确认**：v21→v24 连续 4 轮失败后，识别两个独立问题：
  1. FillBodyPixels 先填深色再由 DrawImage 覆盖的架构依赖 GDI+ 合成可靠性
  2. PushTransparentFrame（ULW 全透明帧）不被 DWM 可靠处理
- **v25 修复**：
  - `FillBodyPixels`：frosted 模式填充 alpha=0（透明）而非 BodyOpacity
  - `EnsureFrostCapture`：用 `SetWindowPos(-99999,-99999)` 物理移出屏幕替代 PushTransparentFrame
- **v25 用户反馈**：✅ 不再全白！四盒均显示毛玻璃效果。❌ 但有之前窗口的残留图像（DWM 缓存未及时清理）。

## v25b 毛玻璃残留修复（提交 2678d53，待复验）
- 在 v25 基础上：Sleep 100→200ms + `RedrawWindow(RDW_UPDATENOW)` 强制桌面重绘窗口区域
- 新增 NativeMethods：`RedrawWindow` + RDW_INVALIDATE/ALLCHILDREN/ERASE/UPDATENOW 常量
- 发布 ds2（exe Aug 30 08:01），双远程已推。
