# 2026-08-01 工作日志

## 进入实现期（Phase -1）
- 规划期临时团队（gstack-desktop-beautify）上下文已不在，队友自动退出，无需 TeamDelete。
- 建立 7 项实现任务清单：Phase -1 安全网 / Phase 0 配置主题加载器 / Phase 1-2 壁纸引擎 / Phase 3 桌面整理 / Phase 4-5 Dock+组件 / Phase 6 AI 接入 / 打包发布。
- 已落地 Phase -1 安全网（Task #1，in_progress）：
  - 项目脚手架 `src/DesktopSuite/`：DesktopSuite.csproj（net8.0-windows + 单文件发布属性，禁用 Trim）、App.xaml/cs、MainWindow.xaml/cs（含「创建基线/恢复」按钮）。
  - `Safety/BackupManager.cs`：导出 HKCU 下 7 个键到时间戳备份目录 + manifest.json。
  - `Safety/RestoreManager.cs`：导入 .reg + 重启 Explorer；FindLatestBackup 按创建时间排序（与批处理目录命名解耦）。
  - `backup.cmd` / `restore.cmd`：纯批处理 L3，不依赖本 App 即可备份/恢复（restore 用 `dir /o-d` 取最新）。

## 关键事实 / 约束
- 沙箱无 .NET SDK，C# 无法本地编译；用户需在本机（Windows + .NET 8 SDK）`dotnet build` / `dotnet publish`。
- 备份键（与安全/恢复一致）：Control Panel\Desktop、Control Panel\Colors、Explorer\Advanced、Shell\Bags、Shell\BagMRU、Windows\DWM、Themes\Personalize。
- 恢复策略：导入注册表 + 重启 Explorer（重建默认可见桌面窗口），使虚拟视图图标隐藏可完全回滚。

## 下一步
- 验证（用户本机）：跑 `backup.cmd` → 改壁纸/隐藏图标 → `restore.cmd` 应恢复。
- 完成后推进 Phase 0（theme.json 加载器，复用 design/theme-schema.json + validate-theme.py 六道闸）。

## 17:08 补充：用户本机无 .NET SDK
- 用户执行 `dotnet build` 报 "No .NET SDKs were found" —— 其 Windows 机器尚未安装 .NET SDK。
- 已调整 `DesktopSuite.csproj`：把 `RuntimeIdentifier/SelfContained/PublishSingleFile/PublishReadyToRun/EnableCompressionInSingleFile/InvariantGlobalization` 从主 PropertyGroup 移除，单文件打包改为 `dotnet publish` 阶段命令。这样 `dotnet build` 走最轻量的依赖框架构建，首次编译无摩擦。
- 交付安装指引：装 .NET 8 SDK (x64, Windows)，验证 `dotnet --version`，再 `cd src\DesktopSuite && dotnet build`（或 `dotnet run`）。
- 发布命令：`dotnet publish -c Release -r win-x64 --self-contained -p:PublishSingleFile=true -p:PublishReadyToRun=true -p:EnableCompressionInSingleFile=true`
