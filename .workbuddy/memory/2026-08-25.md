# 2026-08-25 工作日志

## M4-B 透明度修复 v16 收尾（被中断的发布任务续完）
- 恢复 v16 工作树修改（之前发布命令被用户中断）：`FenceLayer.cs` 最小 alpha 1→20 + `borderA<10` 跳过边框。
- 清理锁（taskkill + rm obj）后 `dotnet publish` 自包含单文件到 `D:\WorkBuddy\ds2`，0 错误。
  ds2 exe 时间戳更新为 Aug 25 09:50（确认 v16 已落地）。
- 提交 + 双远程（gitee/github SSH）推送 v16。
- **真正的根因已定位**：`FenceLayer.cs:1479` `bmp.GetHbitmap()` 参数less 重载不保留 alpha 通道，
  破坏 FillBodyPixels/PremultiplyAlpha 写入的 alpha —— 这才是 v9→v15 七轮"极低 alpha 变白"的源头。
  v16 仅绕过（min alpha=20），未实现"0=真透明"。
- **下一步（待用户确认）**：用 `CreateDIBSection` + 拷贝 premultiplied scan0 替代 `GetHbitmap()`，
  精确保留 alpha → alpha=0 真透出壁纸。
- 待用户重启 ds2 真机复验 v16（0 应为极淡暗色微染而非全白/纯透明）。
