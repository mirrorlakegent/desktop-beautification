# 2026-08-27 工作日志

## ❌ v23 真机复验：PushTransparentFrame 仍然失败
用户截图：四个盒子中"临时"显示正确模糊，"未分类""工具"白/灰白，"工作"半白半暗。
**决定性证据**：同一 _frostBmp 内不同盒子表现不同 → 说明问题不在捕获（否则所有盒子应一致），
但 v23 的 PushTransparentFrame 因 **Bitmap 未零初始化** 导致"透明帧"实际含垃圾 alpha 像素，
窗口并未真正透明 → CopyFromScreen 仍自捕获。

### v24 修复
- **根因**：`new Bitmap(w, h, Format32bppArgb)` 不保证零初始化像素内存；
  GDI+ 可能复用含垃圾数据的缓冲区（包括非零 alpha）→ DIB 拷贝垃圾 → ULW 推送后窗口仍可见
- **修复**：`PushTransparentFrame` 中 `Graphics.FromImage(clearBmp).Clear(Color.Transparent)` 显式清零
- _frostBmp null fallback 已有（v21 加的 else if 分支，保留至今）
- 编译 0 错误；发布 ds2（exe Aug 27 21:29）。提交 + 双远程推送 v24。
- **待用户真机复验**：FrostOpacity=0 纯模糊不再白；四盒一致；换壁纸刷新。
