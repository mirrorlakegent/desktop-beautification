# 2026-08-06 工作日志

## Phase A 真机重跑（vmusr 接上后）
- 用户在 VMware 控制台手工登入 E: 靶机（admin），vmusr 会话代理应已接上。
- 重派 gstack-qa-lead-2（Agent 工具 background）执行：验证 vmusr → 固化自动登录快照 → 同步重跑 Phase A。
- **失败**：运行 41m54s 后 `499 canceled`（被取消，非逻辑/产品错误）。未产出任何真机 verdict；此前 6 BLOCKED/0 FAIL 仍是唯一已知结果。
- **教训/规则**：本环境 background agent 可能在 ~40min 被取消；长耗时 vmrun 真机任务应**拆成更短的 agent**（如先单独验证 vmusr+固化快照，再单独跑 Phase A），避免单次超时被砍。
- 下一步：派更窄范围的 fresh agent——仅 `vmrun -interactive` 探针验证 vmusr + 同步跑 Phase A（暂不做快照固化，留作后续短步骤），尽快拿真 PASS/FAIL。

## 关键进展（gstack-qa-lead-3 回报）
- **vmusr 根因已确认消除**：guest 内 `vmtoolsd.exe`(pid 5848) owner=DESKTOP-3943OKD\admin（vmusr 用户态接上）、Explorer.EXE owner=admin、有 dwm.exe、无 LogonUI；`query session` 显示 `>console admin 1 运行中`；-interactive 程序落在 session 1 交互桌面。上轮 6 BLOCKED 的「无人登过」根因解除。
- **新阻断（测试 harness 自身缺陷，非产品）**：读 gstack-qa-lead-2 的 `evidence/20260806-083149/run.log`，V12 在 08:35:59 起 guest 侧 `Collect-State.ps1 -Label v12-pre` 跑满 240s 超时、state json 未生成 → 回收报"未找到文件" → §0-C 兜底判 BLOCKED。V12 之前已走通（Tools✓/会话visible✓/app部署✓/主进程就绪✓）。嫌疑在 Collect-State 内慢 WMI/事件日志调用（Get-WinEvent 1074、Win32_VideoController），需分段计时定位。**0 条产品回归**。
- **撞车**：gstack-qa-lead-2 与 gstack-qa-lead-3 同跑 E: 靶机（08:00–08:40 全是 gstack-qa-lead-2 产物）。已发 shutdown_request 关停 gstack-qa-lead-2，靶机归 gstack-qa-lead-3 独占。
- **主理人裁决（分步）**：第1步 gstack-qa-lead-3 修 Collect-State 挂死（分段计时定位+单条超时兜底）+ V12 单例真结论；第2步另派短 agent 跑完整 Phase A（V2A/V2B/V11A/V11B/V12/V14）。规避 ~40min 499 取消导致颗粒无收。

## 续：根因分层与 harness 修复（gstack-qa-lead-2 激活后）
- **真元凶（harness 层）**：`Run-Validation.ps1` 把 `Wait-InteractiveSession` 排在 `Invoke-Deploy` 之前；`-RevertBetweenCases` 回滚后 guest 恢复快照里**旧 `Assert-DesktopIcons.ps1`**（line 379 `$samples`/$Samples 大小写撞名 → ArgumentTransformationMetadataException → 100% exit=4），会话探针必崩 → 420s 等不到就绪 → 部署从没发生 → 无 summary.json。两快照（gstack-clean-before-v1-v14 / gstack-clean-autologon）里的 Assert-DesktopIcons=BA8FD6AD、Collect-State=F1DB8592 均旧版。
- **修复（仅 harness，不动产品/快照）**：gstack-qa-lead-2 将 `Invoke-Deploy` 前置到 `Wait-InteractiveSession`（host Run-Validation.ps1 sha C66B274B58E8E8E2，PARSE=OK）。现认领 E: 靶机跑 V12 冒烟（ETA 6–8min）。
- **死代码**：主理人早前加的 `Coerce-Handle`（Assert-DesktopIcons 3155E5DE）零调用点，不作为修复。真实碰撞修复是 `$sampleList`（line 399）规避 `$Samples` 撞名。
- **碰撞已解**：qa-v12-smoke/qa-v12-deploy 均确认未占用 E:；gstack-qa-lead-2 为唯一驱动方。qa-v12-deploy 待命核对 host guest\ 脚本 SHA（Collect-State=0BF1143F、Assert-DesktopIcons=3155E5DE）。
- **待用户确认**：主机另有 D: VM（D:\Program Files\VMwar_xitongwenjian\win10\Windows 10 x64.vmx, pid 15392, 自 08:55）在跑，非 E: 靶机，疑似无关/既有，全程不碰。
- 下一步：等 gstack-qa-lead-2 的 V12 真裁决 → 门槛用例（V2A/V2B/V11A/V11B/V12/V14）真机裁决 → 汇编报告落盘 deliverables/gstack/。

## 续2：断言阻塞 100% 确诊并真机验证通过（qa-v12-deploy）
- **根因铁证**：guest L67 `[int] $Samples=1`（param，[int]约束）与 L379 `$samples = New-Object ArrayList` 因 PS 大小写不敏感撞名 → `ArgumentTransformationMetadataException`（"无法将 ArrayList 转换为 Int32"）。错误产物栈帧为 `<ScriptBlock>`（顶层脚本域），非 Get-IconSample 内 → 句柄/IntPtr 从未涉及。早前"L374/L376 句柄"理论是错的。
- **Coerce-Handle 确证为死代码**：全文件仅 line 223 定义，0 调用点；验证跑零调用即通过 → 句柄转换从未需要。建议删除（若接线会引入静默 [IntPtr]::Zero 兜底，掩盖真实窗口链失败）。主理人接受，列为事后清理项。
- **真机验证通过**：部署 host 3155E5DE + SHA 匹配，实时桌面会话跑断言 → assertExit=0、verdict=visible、reasonCode=ws-visible-set、sampleCount=3（progman=0x10120/defView=0x10122/listView=0x10124/workerWCount=11）。产物 _pull/assert-fixverify.json、_verify2.txt。
- **残留环境阻断**：~60s 执行墙会强杀同步多分钟 Run-Validation.ps1；Start-Process/schtasks 被策略拦，无法 detach。故完整 V12 用例仍未跑出 PASS/FAIL，需后台执行（run_in_background）存活。
- **测试床卫生**：guest 曾漂移到未知旧版 BA8FD6AD（既非 21AD6AA8 也非 host）；10:54-10:57 VMware Tools 掉线+guest 重启（自动登录 session1）。每用例跑前须重部署+SHA校验。
- 下一步：gstack-qa-lead-2 以后台方式跑 V12（harness 已前置 Invoke-Deploy），拿完整 PASS/FAIL。

## 续3：Phase A 首次完整跑暴露 harness 两新缺陷（gstack-qa-lead-2）
- 首跑 20260806-110336 出真数据但被两 harness 缺陷污染，结论不可用；已修并于 11:20:46 重跑（runner pid 17472，harness sha=A60FC9B8112FEC85，E: 仍独占）。
- **缺陷3｜恒假 FAIL**：Run-Validation.ps1 L775 `Assert-Setting` 缺内层括号 `if (Test-Prop ... -and (Get-Prop ...))` → PS 把 `-and` 当 Test-Prop 实参，返回值恒真；采集器总输出空 parseError 字段 → 每次都误报「settings.json 解析失败」FAIL。六用例约调 15 次，足以污染全部结论。已改 `if ((Test-Prop ...) -and (Get-Prop ...))`。
- **缺陷4｜重启从未发生**：`Restart-Guest` 用 `shutdown /r /t 0`，/t 0 不隐含 /f，前台应用（被测 DesktopSuite.exe）可否决关机 → V2A/V2B 300s 内未下线 → BLOCKED。已改 `shutdown /r /t 0 /f`（logoff 也加 /f），并把 Out-Null 吞掉的返回码写 run.log。
- **产品线索**：应用疑似否决系统关机，值得单独立 issue。
- 被污染轮（勿采信）：V2A FAIL（图标隐藏 PASS、自启前置 PASS、settings 误判 FAIL、下线 BLOCKED）、V2B BLOCKED。路径 evidence/20260806-110336/summary.json。
- 现状：gstack-qa-lead-2 独占 E: 重跑，待报完整六用例结论。零产品失败证据持续增强（所有 FAIL/BLOCKED 均 harness 产物）。

## 续5（主理人汇编：V11A 根因假设收窄 + Phase B 启动）

- **交叉关联发现（重要）**：V11A「日志无 SessionEnding 行、恢复闩锁命中 0 次」与 harness 缺陷④排查中的副发现「DesktopSuite.exe 否决 `shutdown /r /t 0`（无 /f）致 300s 未下线」**是同一根因的两面**。
  - 关键推论：**无 SessionEnding/WM_QUERYENDSESSION 处理器的进程不会被 Windows 赋予否决关机的能力**，会被直接终止。能否决 → 处理器已注册且已被调用。
  - 因此原判断「处理器未生效」不准确。修正假设：处理器**体内中断**，未走到恢复逻辑就返回 cancel。三候选：①体内抛异常（恢复未执行 + 日志因异常/缓冲未 flush 而空）②阻塞超时 ③应答语义写反返回 cancel。
  - 排查建议：不要查"为何没注册"（已被否决）。在处理器入口/出口加**无缓冲落盘**日志（异常路径也写），复现关机查中断点；若日志仍空重点查①。
  - 已写入报告 §2「交叉关联分析」并更新行动清单 #1。假设未经实验验证，标注为起点而非结论。
- **教训**：两位成员各观测到一面但未交叉，主理人汇编时串联才发现矛盾。多成员协作时，"局限/副发现"段落也要与主发现做交叉校验，别只汇总不碰撞。
- **Phase B 已启动**：派 gstack-qa-lead-2-2 跑此前从未执行的剩余用例（V1/V3-V10/V13），E: 靶机，harness sha A60FC9B8112FEC85，要求分两批（各5个）报告以规避 ~40min agent 取消墙。目的：让产品团队一次性拿全谱系缺陷，避免"修完门槛又冒新问题"的往返。

## 续4：Phase A 六用例首个可信裁决（1 PASS / 3 FAIL / 2 BLOCKED）
- 跑完于 ~13:48，E: 释放。汇总 evidence\20260806-124400\summary.json。harness sha A60FC9B8112FEC85（部署前置+断言括号修复+/f 重启+VIX 就绪）。
- 判读铁律（qa-v12-deploy）：好版断言=exitCode=0/visible/ws-visible-set 或 1/hidden/ws-visible-clear（合法）；坏版=exitCode=4/script-exception/ArrayList→Int32。**本轮所有断言均合法输出 → Invoke-Deploy 成功、零 stale-build 产物**。
- 裁决：
  - **V2A FAIL**：手动支线重启后图标仍 hidden（合法输出），用例期望 visible。手动支线关机时 app 未运行→无法触发 SessionEnding 恢复。产品缺陷 or 测试预期偏差，待产品负责人拍板。
  - **V2B PASS**：--background 自启支线正常。
  - **V11A FAIL（🔴真实产品缺陷 P1-2）**：真实关机→重登录图标未恢复；日志无 SessionEnding 行；恢复闩锁命中 0 次（期望1）→ SessionEnding 处理器未生效，用户关机拿空桌面。
  - **V11B BLOCKED（🟡基础设施）**：--background 支线"首次重启未发生"，重启机制待查，产品未触达。
  - **V12 BLOCKED（🟡基础设施）**：注销后 420s 内未自动登录回会话（自动登录配置问题）；注销前产品状态正确。
  - **V14 FAIL（🔴真实产品缺陷 high）**：进程强杀后 settings.json 损坏（"未损坏"步骤 high 失败）；其余均 PASS。
- 结论：基础设施修好后首个可信裁决，**首次暴露真实产品缺陷 V11A + V14**；V11B/V12 为基础设施 BLOCKED 可复测；V2A 待澄清。**放行 No-Go**。
- 报告落盘：deliverables/gstack/pre-release-check-desktopsuite-phase3-2026-08-06.md。
- 副发现：DesktopSuite.exe 疑似否决系统关机（shutdown /r /t 0 无 /f 时 300s 未下线），已用 /f 绕过，是否预期待产品确认（或关联 V11A）。
- 待办：产品团队修 V11A/V14；QA 基础设施修 V11B 重启路径+V12 自动登录后复测；重拍基础快照消探针单点；删死代码 Coerce-Handle；E: 空闲后补 collector 独立复测。D: VM（pid 15392）归属仍待用户确认。
